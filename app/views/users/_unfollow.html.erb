<%= form_for(current_user.followships.find_by_followed_id(@person), :html => { :method => :delete } ) do |f| %>
	<div class="" ><a href="javascript:document.forms['new_followship'].submit(); return false;"><%=f.submit "Unfollow" %></a></div>
<% end %>